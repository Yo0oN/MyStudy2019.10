<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis1">
	<select id="list" resultType="model1.DeptTO">
		select seq, subject, writer, date_format(wdate, '%Y-%m-%d') wdate, hit, datediff(now(), wdate) wgap
		from board1
		order by seq desc
	</select>
	<select id="selectList2" parameterType="String"
		resultType="model1.ZipcodeSearchTO">
		select *
		from zipcode
		where dong like #{dong}
	</select>
	<select id="selectList2" parameterType="String"
		resultType="model1.ZipcodeSearchTO">
		select *
		from zipcode
		where dong like #{dong}
	</select>
	<select id="selectList2" parameterType="String"
		resultType="model1.ZipcodeSearchTO">
		select *
		from zipcode
		where dong like #{dong}
	</select>
	<select id="selectList2" parameterType="String"
		resultType="model1.ZipcodeSearchTO">
		select *
		from zipcode
		where dong like #{dong}
	</select>
	<select id="selectList2" parameterType="String"
		resultType="model1.ZipcodeSearchTO">
		select *
		from zipcode
		where dong like #{dong}
	</select>
	<select id="selectList2" parameterType="String"
		resultType="model1.ZipcodeSearchTO">
		select *
		from zipcode
		where dong like #{dong}
	</select>
	<select id="selectList2" parameterType="String"
		resultType="model1.ZipcodeSearchTO">
		select *
		from zipcode
		where dong like #{dong}
	</select>
	<select id="selectList2" parameterType="String"
		resultType="model1.ZipcodeSearchTO">
		select *
		from zipcode
		where dong like #{dong}
	</select>
	<select id="selectList2" parameterType="String"
		resultType="model1.ZipcodeSearchTO">
		select *
		from zipcode
		where dong like #{dong}
	</select>

</mapper>